import { Direction as TooltipDirection } from "leaflet";

export const DEFAULT_ZOOM = 2;
export const MAX_BOUNDS_VISCOSITY = 1;
export const MAX_BOUNDS_TOLERANCE = 0.8;

export const IIIF_TILE_SUFFIX_FORMAT =
  "{region}/{size}/{rotation}/{quality}.{format}";

export const ZOOM_ITERATION_CHECK_MAX_ATTEMPTS = 5;
export const DEFAULT_TILE_SIZE_PIXELS = 256;
